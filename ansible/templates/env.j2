PROJECT_NAME="{{ item.name }}"
FIRST_DATE='{{ item.first_date }}'
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD="{{ clickhouse_password }}"
METRIKA_COUNTER={{ item.counter }}
METRIKA_KEY={{ metrika_key }}
LOG_ENABLE="true"
